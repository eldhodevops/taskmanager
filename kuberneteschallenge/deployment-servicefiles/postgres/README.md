#Location: E:\kubernetestask\postgres in my windows machine 

Create deployment and service for postgres using the below command:
```
kubectl.exe create -f deployment.yml
```
```
kubectl.exe create -f service.yml
```
